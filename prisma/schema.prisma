generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "sqlite"
    url      = "file:./data.db"
}

model image {
    id        Int     @id @default(autoincrement())
    file_name String
    deleted   Boolean @default(false)
}
